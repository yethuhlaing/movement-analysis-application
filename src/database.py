# import sqlite3
# import json

# # Create an SQLite database and table
# conn = sqlite3.connect('my_database.db')
# cursor = conn.cursor()
# cursor.execute(
#     'CREATE TABLE IF NOT EXISTS history (id INTEGER PRIMARY KEY, data JSON)'
# )

# json_data = json.dumps(my_dict)

